x=int(input("Podaj liczbę"))
y=int(input("Podaj Liczbę"))



while y>=x:
    x += 1
    if x % 2 != 0:
        continue
    print(f"x = {x}")
    x = x + 1

while x>y:
    if y % 2 != 0:
        x += 1
        continue
    print(f"y = {y}")
    y += 1
